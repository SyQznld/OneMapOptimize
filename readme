放在外部配置文件中读取：
1、SD卡根路径下
  1)mapdata中
    1.1.1 存放切片数据，命名需要清晰，尽量用英文，例如hj_yx,cg_tdlyxz,里层文件夹统一用Layers
    1.1.2 MapConfig.txt,地图配置，格式为正确的json，包括原有的中心点、范围、比例尺等
    {
    	"arcgisTilesConfig": {
    		"originXY": [-5123200, 10002100],        //中心点
    		"mapExtent": [453498.17450728768, 3852284.0619481243, 481451.04058256379, 3871561.5554315117],     //最大最小范围
    		"resolutions": [33.866734400135471, 16.933367200067735, 8.4666836000338677, 4.2333418000169338, 2.1166709000084669, 1.0583354500042335, 0.52916772500211673, 0.26458386250105836],//比例尺
            "center":[]      //地图中心，不知道的情况下为空
    	},

    	"mapZoomConfig": {
    		"maxZoom": 6,     //最大层级
    		"minZoom": 0,     //最小层级，即初始加载层级
    		"zoom": 0
    	},
        "baseLayer":"hj_yx",   //初始加载的图层名称
        "proj4":"+proj=tmerc +lat_0=0 +lon_0=114 +k=1.000000 +x_0=500000 +y_0=0 +a=6378140 +b=6356755.288157528 +units=m +no_defs",     //定位参数，根据需要调换经度
        "offsetX":0,           //左右偏移：正数此时定位向右偏移，负数此时定位向左偏移（例如：此时定位地点在目标地右侧200米，应为-200）
        "offsetY":0            //上下偏移：正数此时定位向上偏移，负数此时定位向下偏移（例如：此时定位地点在目标地上侧200米，应为-200）
    }


2、一张图配置
  1)图层树目录
    数据清单.txt（名字必须包含数据清单，可以是惠济区数据清单/东城区数据清单），包含不同种类不同需求多种基本配置，多个参数分开使用时需要用空格隔开，不同数据需要换行
                     程序中默认加载配置文件中的第一个图层数据，所以将作为地图的数据放在最前面

    影像数据             //数据分类
    惠济影像 hj_yx       //不需点查的图层只需要包含图层名称、命名


    规划数据
    总规图              hj_zgt         raster                //需要点查的数据需要写上数据类型，raster为切片，vector为矢量
    基本农田            hj_jbnt        raster
    建设用地管制区       hj_jsydgzq     raster
    乡                 XIANG          vector  yellow         //矢量数据加载可以自己配置颜色，可以为常见颜色，或者rgb值
    村                 CUN            vector  red     XZQMC  //矢量数据可以添加标注，配置字段名称
  2)主页模块显示
    ModuleConfig.txt,文本文件中写入主页需要显示的模块名称（目前没有做分隔限制，代码中判断用的是包含contains）
    图层 测量 搜索 分析 采集 设置 更多
  3)点查json
    点查文件，一般为格式是utf-8类型的txt文件，文件命名与图层命名保持一致
  4)地名地址
    DMDZ.txt，固定属性字段
  5)标注（距离量算模块关联）
    BZ.txt，标注模块保存的图斑，包括线、面不同类型
  6)图斑列表(采集模块关联)
    GATHER.txt,显示保存在本地的采集列表







更改svn地址Ip
svn ls  https://192.168.2.55/svn/AiPuRe_Project
然后 p
再然后是 用户名密码








20191112  韩总开会提出改进意见：
1，数据导出格式，导出dat格式。
2，增加构面功能，构面先以列表形式，勾选构面。后续增加捕捉功能。
3，点构面后，再从新对采集点进行编号，点号递增功能保留。
4，参考手机CAD的功能，增加对应必须功能。
5，后续增加导入shp、dwg格式数据功能。
6，增加软件权限注册功能，手机号+验证码+身份证后6位+人名+平板S/N码。
7，对构面增加面属性，对面进行拍照、录像等功能，并有位置限制的功能。
8，增加生成电子报告功能，并增加微信分享功能。
9，参考工程之星的功能。
10，参考平板自带测量软件，增加点、线、面、地图、导航功能（直接调用百度地图）
11，集成各个平板版本的功能，形成一个完善的产品。只保留一个版本。


















monkey测试代码
adb shell monkey -v -v -v -s 8888 --throttle 300 --pct-touch 30 --pct-motion 25 --pct-appswitch 25 --pct-majornav 5 --pct-nav 0 --pct-trackball 0 -p com.oneMap.module.debug 100 >D:\monkey.txt


